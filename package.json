{
    "name": "sucoot-scaform",
    "private": true,
    "scripts": {
        "dev": "cd frontend && npm start",
        "build:frontend": "cd frontend && BUILD_PATH=../public/app npm run build",
        "serve": "php artisan serve",
        "setup": "composer install && php artisan migrate --seed && yarn build:frontend",
        "fresh": "php artisan migrate:fresh --seed"
    }
}
